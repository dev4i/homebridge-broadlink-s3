{
	"pluginAlias": "Broadlink",
	"pluginType": "platform",
	"singular": true,
	"schema": {
		"type": "object",
		"properties": {
			"name": {
				"title": "There are no settings to configure. Simply unlock the Smart Hub in the app and it should be discovered.",
				"type": "string",
				"required": true,
				"default": "There are no settings to configure. Simply unlock the Smart Hub in the app and it should be discovered."
			}
		}
	}
}
